{
  "name": "@ai-firewall/shared-auth",
  "version": "0.1.0",
  "private": true,
  "description": "Shared authentication primitives for AI Firewall: token file IO, loopback callback server, web-login URL builder. Zero runtime deps.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node --test --experimental-test-coverage dist/test/*.test.js"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "@types/node": "^20.0.0"
  }
}
