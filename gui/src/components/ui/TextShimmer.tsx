/**
 * DEPRECATED — forwards to AfTextShimmer from the loader system.
 *
 * New callers should import from `../loaders` directly:
 *   import { AfTextShimmer } from "../loaders/AfTextShimmer";
 *
 * This shim stays until every caller has migrated.
 */
export { AfTextShimmer as TextShimmer } from "../loaders/AfTextShimmer";
