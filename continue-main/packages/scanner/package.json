{
  "name": "@ai-firewall/scanner",
  "version": "0.1.0",
  "private": true,
  "description": "Pure-function security scanners: secrets, PII, entropy, prompt injection, context adjustment",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
