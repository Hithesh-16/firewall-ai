/**
 * Utility to check if a user is an AI Firewall team member.
 * Re-export so callers can import from the modern filename.
 * Phase H.H7a removed the deprecated `isContinueTeamMember` alias.
 */
export { isFirewallTeamMember } from "./isContinueTeamMember.js";
